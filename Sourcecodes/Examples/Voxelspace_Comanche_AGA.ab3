; XTRA
; Embedded .xtra Header
; 
; General Info
; -------------------------------------------------------
; ExePath         = "Blitz3:Sourcecodes/Bin"
; ExeFile         = "dbl_VoxelSpace"
; CreateIcon      = 1
; Residents       = "all.res"
; 
; Compiler
; -------------------------------------------------------
; StringBuffer    = 100000
; MakeSmallest    = 1
; FuncOptimize    = 1
; Version         = 0.0.0
; NumberOfBuilds  = 1192
; 
; Debugger
; -------------------------------------------------------
; CliArgs         = ""
; StackSize       = 8191
; RuntimeDebug    = 1
; DebugInfo       = 0
; CreateDbgFile   = 0
; OverflowCheck   = 0
; AssemblerCheck  = 0
; InterruptCheck  = 1
; AutoRun         = 1
; 
; Editor
; -------------------------------------------------------
; CursorLine      = 7
; CursorColumn    = 11
; LabelSearch     = "camera\angle"
; LabelRemark     = 0
; LabelAll        = 1
; LabelCase       = 0
; LabelPosition   = 0
; 
; Blitz Objects
; -------------------------------------------------------
; Max File        = 100
; Max GadgetList  = 100
; Max Shape       = 200
; Max Bank        = 1
; Max MenuList    = 100
; Max GTList      = 100
; Max BitMap      = 10
; Max Screen      = 5
; Max IntuiFont   = 10
; Max Window      = 100
; Max Palette     = 4
; Max BlitzFont   = 1
; Max IconInfo    = 1
; Max MUIObject   = 50
; Max AsyncReq    = 4
; Max Req-Lib     = 5
; Max GTMenuList  = 100
; Max Console     = 5
; Max TCPSock     = 5
; Max Tape        = 5
; Max TagList     = 5
; Max Database    = 16
; Max Sound       = 10
; Max MedModule   = 8
; Max Buffer      = 10
; Max Queue       = 10
; Max Sprite      = 20
; Max Module      = 5
; Max Slice       = 10
; Max Page        = 4
; Max CopList     = 10
; Max PTModule    = 5
; Max Anim        = 10
; Max NChunky     = 50
; Max Chunky      = 20
; Max Stencil     = 5
; Max XBSound     = 10
; /XTRA
; --
; -- simple VoxelSpace-Demo conversion
; --
; -- inspired by VoxelSpace by Sebastian Macke
; -- github: https://github.com/s-macke/VoxelSpace/blob/master/VoxelSpace.html
; --
OPTIMIZE 5 ; - FPU on strongly advised
SYNTAX 6

#display_width   = 320
#display_height  = 200
#display_depth   = 8

NEWTYPE.input
  forwardbackward.q ; change position
  leftright.q       ; change angle
  updown.q          ; change height
  tilt.b            ; tilt of view (horizon)
End NEWTYPE

NEWTYPE.camera
  x.q
  y.q
  height.q
  angle.q
  horizon.q
  distance.w
End NEWTYPE

NEWTYPE.map
  posx.w
  posy.w
  scalefactor.q
  width.w
  height.w

  scalefx.q
  scalefy.q

  stepping.b
  shift.w
End NEWTYPE

DEFTYPE.b db
DEFTYPE.input _input
_input\forwardbackward = 6.0

DEFTYPE.camera camera
camera\x = 512.0
camera\y = 400.0
camera\height = 96.0;78.0
camera\angle = 0.0
camera\horizon = 100.0
camera\distance = 800

DEFTYPE.map map
map\width      = 1024
map\height     = 1024
map\shift      = 10    ; 1024
map\stepping   = 8  ; reducable horizontal resolution

Dim altitude.b(map\width * map\height)
Dim color.w(map\width * map\height)
Dim hiddeny.l(#display_width)
Dim SinLUp.q(360)
Dim CosLUp.q(360)

Macro DrawBackground
  Cls 14
End Macro

Statement DrawVerticalLine{x.l, ytop.l, ybottom.l, color.l}
  SHARED map
  If ytop < 0 Then ytop = 0
  If ytop > ybottom Then Statement Return
  QAMIGA
  If map\stepping > 1
    Boxf x,ytop,x+map\stepping,ybottom,color
  Else
    Line x,ytop,x,ybottom,color
  EndIf
  BLITZ
End Statement

Statement Render{}
  SHARED camera, map, altitude(), color(), hiddeny(), SinLUp(), CosLUp()

  sinang.q = SinLUp(camera\angle)
  cosang.q = CosLUp(camera\angle)

  ;-- reset hidden lines
  For x.l = 0 To #display_width
    hiddeny(x) = #display_height
  Next x

  ;-- draw from front to back
  deltaz.f = 1.0
  For z.q = 1 To camera\distance Step deltaz
    ;-- 90 degree field of view
    plx.q =  -cosang * z - sinang * z
    ply.q =   sinang * z - cosang * z
    prx.q =   cosang * z - sinang * z
    pry.q =  -sinang * z - cosang * z

    dx.q = (prx - plx) / (#display_width / map\stepping)
    dy.q = (pry - ply) / (#display_width / map\stepping);height)

    plx + camera\x
    ply + camera\y

    invz.f = 1.0 / z * 240.0

    For x = 0 To #display_width Step map\stepping

      mapoffset.l = ((Int(ply) AND (map\height-1)) LSL map\shift) + (Int(plx) & (map\width-1))
      alt.w = altitude(mapoffset)
      color.l = color(mapoffset)

      yonscreen.l = (camera\height - alt) * invz + camera\horizon

       ;- draw the vertical block
      DrawVerticalLine{x, yonscreen, hiddeny(x), color}
      If (yonscreen < hiddeny(x)) Then hiddeny(x) = yonscreen

      plx + dx
      ply + dy
    Next x

    deltaz + 0.005

  Next z

End Statement



Function DoubleBuffer{}
SHARED db
    VWait
    DisplayBitMap 0,db
    db = 1 - db
    Use BitMap db
    BitMapOutput db
    UnQueue db
End Function

.MAIN
; -- MAINPROGRAM ------------------------------------------------------------
WBStartup

; --
; --  preload map datas
; --
NPrint "generating tables..."
Gosub generate_tables

; --
; -- alloc bitmaps for doublebuffering
; --
BitMap 0,#display_width,#display_height,#display_depth
BitMap 1,#display_width,#display_height,#display_depth


; --
; -- open the display
; --
NPrint "creating display..."
InitCopList 0,64,#display_height,$10000 + #display_depth,8,256,0

BLITZ
CreateDisplay 0
DisplayPalette 0,0
Queue 0,15 : Queue 1,15

Mouse On : BlitzKeys On
; --
; -- main loop
; --
oldtime.l = 0 : current.l  = 0 : timediff.q = 0.0 : oangle.q = 0 : db = 0
Repeat
  current.l = Timer

  ; --
  ; -- switch buffer
  ; --
  DoubleBuffer{}

  lastkey.w = BlitzKey

  ; --
  ; -- render background
  ; --  
  !DrawBackground

  ; --
  ; -- render landscape
  ; -- draw landscape  
  ; --
  RunErrsOff
  Render{}
  RunErrsOn

  ; --
  ; -- render the map
  ; -- 
  QBlit db,0,map\posx,map\posy
  posx.w = map\posx + Int(camera\x * map\scalefx * map\scalefactor)
  posy.w = map\posy + Int(camera\y * map\scalefy * map\scalefactor)
 
  ; -- position marker  
  Plot posx,posy,Rnd(16)

  ; --
  ; -- update text display
  ; --
  Colour 25,14
  Locate 0,0  : Print "1-4: ", map\stepping,"x",map\stepping
  Locate  0,1 : Print"R,F: Height | Q,E: Nose"
  Colour 27,14
  Locate 0,2  : Print "Speed: ", _input\forwardbackward
  Format "##0"
  Locate 10,2 : Print "Height:", Str$(camera\height)
  Locate 22,2 : Print "View:" + Str$(camera\angle * (180/Pi)) + Chr$(176)
  Format "####.0"
  Locate 0,3  : Print "x:" + Str$(camera\x) + "  y:" + Str$(camera\y)

  Colour 25,0
  Format ""
  Locate 0,24 : Print lastkey
  Format "##.0" : Locate 32,24 : Print "FPS:", fps.q
  Format ""

  ; ----------------------------------------- update camera  
  fps.q = 50 / (current - oldtime)
  timediff = (current-oldtime) * 0.05 : oldtime = current

  ; speed
  If _input\forwardbackward <> 0
    camera\x - _input\forwardbackward * Sin(camera\angle) * timediff
    camera\y - _input\forwardbackward * Cos(camera\angle) * timediff
  EndIf

  ; looking angle
  If _input\leftright <> 0
    oangle = camera\angle
    camera\angle + _input\leftright * 0.1 * timediff

    If Abs(camera\angle) > 2 * Pi Then camera\angle = 0
  EndIf

  ; height
  If _input\updown Then camera\height + _input\updown * timediff

  ; horizon
  If _input\tilt Then camera\horizon + _input\tilt * (2 * timediff)

  ; -- collision detection, auto height up
  mapoffset.l = (Int(camera\y) AND (map\width-1)) LSL map\shift + (Int(camera\x) AND (map\height-1))
  If altitude(mapoffset) + 10.0 > camera\height
    camera\height = altitude(mapoffset) + 10.0
  EndIf

  ; -- sync with map on screen
  If camera\x < 0 Then camera\x = map\width
  If camera\y < 0 Then camera\y = map\height
  If camera\x > map\width Then camera\x = 0 ;- map\width
  If camera\y > map\height Then camera\y = 0

  ; ----------------------------------------- event handler
  _input\leftright = -MouseXSpeed / (#display_width LSR 5)
  _input\updown = -MouseYSpeed / (#display_height LSR 5)
  ;camera\horizon = 100.0 + (MouseYSpeed / #display_height) * 96.0

  If Joyb(0) = 1 Then _input\forwardbackward = 2.0
  If Joyb(0) > 1 Then exit.b = True

  If lastkey <> -1
    ; check W,A,S,D and cursor keys
    If lastkey = 115 OR lastkey = 29 Then _input\forwardbackward + -3.0
    If lastkey = 119 OR lastkey = 28 Then _input\forwardbackward + 3.0
    If lastkey = 100 OR lastkey = 30 Then _input\leftright = -1
    If lastkey = 97  OR lastkey = 31 Then _input\leftright = 1

    ; control height
    If lastkey = 102 Then _input\updown = -2.0           ; f
    If lastkey = 114 Then _input\updown = 2.0            ; r

    ; nose up / down
    If lastkey = 101 Then _input\tilt = 1
    If lastkey = 113 Then _input\tilt = -1
       
    ; control details
    If lastkey = 49 Then map\stepping = 1                ; F1
    If lastkey = 50 Then map\stepping = 2                ; F2
    If lastkey = 51 Then map\stepping = 4                ; F3
    If lastkey = 52 Then map\stepping = 8                ; F3


    If lastkey = 27 Then exit = True
  EndIf

Until exit = True
BlitzKeys Off
AMIGA

NPrint "cleanup everything..."
FlushEvents
Free BitMap 0
Free BitMap 1
Free Palette 0
Free Shape 0
NPrint "end"
End

;=======================================================================================
.generate_tables
  ; lookup tables
  NPrint "creating lookup tables..."
  For aaa.q = 0 To 2 * Pi Step 0.1
    SinLUp(aaa) = Sin(aaa)
    CosLUp(aaa) = Cos(aaa)
  Next aaa

  ; -- load the map / colormap
  NPrint "loading map data..."
  BitMap 2,150,150,8 : LoadBitMap 2,"/data/voxel/C1W_low.iff",0
  BitMap 3,150,150,8 : LoadBitMap 3,"/data/voxel/D1_low.iff"

  map\scalefactor = 0.25
  map\scalefx = 150 / map\width * 1.0
  map\scalefy = 150 / map\height * 1.0

  NPrint "reading colormap and heightmap..."
  RunErrsOff
  w.w = 0 : h.w = 0 : ows.w = -1 : ohs.w = -1
  For i.l = 0 To (map\width-1) * (map\height-1)
    h = i LSR map\shift

    ws.w = w * map\scalefx
    hs.w = h * map\scalefy

    ;-- get color values out of colormap and heightmap
    ;only read data from bitmap if a new pixel is to be read
    If (ws <> ows) OR (hs <> ohs)
      Use BitMap 2 : c.w = Point(ws, hs)
      Use BitMap 3 : a.w = Point(ws, hs) AND $FF
    EndIf

    color(i) = c : altitude(i) = a

    w + 1 : If w = map\width Then w = 0
    ows = ws : ohs = hs
  Next i
  RunErrsOn

  ; --
  ; -- free temp bitmaps
  ; --
  Free BitMap 2
  Free BitMap 3

  ; --
  ; -- create the scaled map
  ; --
  NPrint "rendering scaled map..."

  _shapeWidth.w = map\width * map\scalefx * map\scalefactor
  _shapeHeight.w = map\height * map\scalefy * map\scalefactor
  BitMap 0,_shapeWidth, _shapeHeight,#display_depth
  For x.w = 1 To _shapeWidth
    For y.w = 1 To _shapeHeight

      tmpx.q = (x / map\scalefx) / map\scalefactor
      tmpy.q = (y / map\scalefy) / map\scalefactor
      offset.l =  Int(tmpy) LSL map\shift + Int(tmpx)

      Plot tmpx, tmpy, color(offset)
    Next y
  Next x
  GetaShape 0,0,0,_shapeWidth-1, _shapeHeight-1
  Free BitMap 0
  map\posx = #display_width - (1.0 * 150 * map\scalefactor) - 3
  map\posy = 3

Return


